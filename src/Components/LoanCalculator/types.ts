export interface chartInputData {
    chartItemData: {
        value: number;
        color: string;
    };
}

export interface chartOutputItem {
    label: string;
    color: string;
    amount: number;
    share: number;
}
